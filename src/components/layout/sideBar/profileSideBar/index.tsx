import classes from "./profileSideBar.module.scss";

const ProfileSideBar: React.FC = (): JSX.Element => {
  return <div className={classes.SideBar}>PROFILE SIDEBAR</div>;
};

export default ProfileSideBar;

import React from "react";
import Head from "next/head";

const Auth: React.FC = (): JSX.Element => {
  return (
    <div>
      <Head>
        <title>ثبت نام</title>
      </Head>
    </div>
  );
};

export default Auth;
